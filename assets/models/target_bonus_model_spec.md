# Bonus Target Model Spec

- Same ring shape as other targets but:
  - Slightly larger scale.
  - Glowing/pulsing colors (neon, gold, or electric blue).
- You can use emissive texture if Hytopia supports it; otherwise just bright colors.

Export as:
`models/target_bonus.glb`
