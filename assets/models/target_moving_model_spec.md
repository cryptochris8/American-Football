# Moving Target Model Spec

Can reuse the basic target mesh with a slightly different color or frame.

- Size: same as basic target.
- Visual variation: maybe a subtle animated texture or brighter outline.

Export as:
`models/target_moving.glb`
